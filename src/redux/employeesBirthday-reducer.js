

let initialState = {

};

const employeesBirthdayReducer = (state = initialState, action) => {
  switch (action.type) {


    default:
      return state;
  }
};



export default employeesBirthdayReducer;
